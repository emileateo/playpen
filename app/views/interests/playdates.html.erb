<h1>Play dates</h1>
<div class="cards" id="playdates">
    <% @playdates.each do |playdate| %>
    <div class="card-category" style="background-image: linear-gradient(rgba(0,0,0,0.3), rgba(0,0,0,0.3)), url(<%= cl_image_path(pet.photo.key) %>)">
      <% end %>
    </div>
